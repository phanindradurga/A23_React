export class InviteData {
    constructor(isSelected, emailMob, lastInvite, acceptDate, status) {
        this.isSelected = isSelected
        this.emailMob = emailMob
        this.lastInvite = lastInvite
        this.acceptDate = acceptDate
        this.status = status
    }
}